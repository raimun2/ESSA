library(tidyverse)

streams <- readRDS("data/streams_rai.rda")

githubURL <- "https://github.com/raimun2/ESStream/blob/master/data/streams_rai.rda?raw=TRUE"
potato <- load(url(githubURL))

save(version = 3)

streams <- flujos

save(streams, file="data/streams_rai2.rda", version = 3)


streams <- readRDS(url("githubURL"))


library(RCurl)
## paste URL to make it easier to read code (cosmetic!)
dat_url <- paste0("https://raw.github.com/richierocks/",
                  "learningr/master/data/english_monarchs.rda")
f <- getBinaryURL()
L <- load(rawConnection(f))




archivo <- getURL("https://raw.github.com/raimun2/ESStream/blob/master/datasets/streams_rai.RData")
y <- read.csv(text = x)

df2 <- readRDS(file="df.Rda")


streams <- devtools::source_url("https://github.com/raimun2/ESStream/blob/master/datasets/streams_rai.RData?raw=TRUE")

readings <- streams$streams